name: Integrate homepage launch links

on:
  push:
    branches:
      - agent/homepage-launch

permissions:
  contents: write

jobs:
  patch-homepage:
    if: ${{ !contains(github.event.head_commit.message, '[homepage-patched]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/homepage-launch
          fetch-depth: 0
      - name: Patch launch files
        run: python3 scripts/integrate-homepage.py
      - name: Commit launch integration
        run: |
          if git diff --quiet; then
            echo "No launch changes needed."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html working-file/data.js
          git commit -m "[homepage-patched] Integrate public launch into site"
          git push origin HEAD:agent/homepage-launch
